/** Shared constants safe to import in both server and client code. */
export const MAX_PUZZLE_ATTEMPTS = 3;

/** Hidden Word uses a stricter 2-game limit independent of MAX_PUZZLE_ATTEMPTS. */
export const WORD_CRACK_MAX_ATTEMPTS = 2;
